# Flask
SECRET_KEY=change-this-in-production
FLASK_DEBUG=true
PORT=5000

# Optional server-side default API keys.
# Users can also enter their own key directly in the UI per-request,
# which takes priority over these defaults.

# NVIDIA NIM (https://integrate.api.nvidia.com/v1, model: openai/gpt-oss-20b)
NVIDIA_API_KEY=

# Anthropic Claude (https://api.anthropic.com/v1/messages)
ANTHROPIC_API_KEY=
